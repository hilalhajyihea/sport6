# ספורט 6

אתר סיקור כדורגל מטייבה.

## ניהול

הכתובת: `/admin`

בפעם הראשונה נוצר מנהל ראשון. אחר כך כל כתב נכנס עם שם משתמש וסיסמה.

## משתנים בורסל

- `DATABASE_URL` — מסד ברנדר
- `SESSION_SECRET` — מחרוזת סודית להתחברות
- `BLOB_READ_WRITE_TOKEN` — אחסון תמונות בורסל
